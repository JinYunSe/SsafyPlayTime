# 2026.03.04 작업 인수인계

## 1) 세션 기본 정보
- 날짜: 2026-03-04
- 브랜치: `CL/feat/{S14P21D104-97}-item-prototype`
- 작업 위치: `client/Assets/_Project/Scripts/Game/`
- 목표: 아이템 프로토타입 VFX/SFX 동작 안정화 + MCPForUnity 중복 충돌 제거 후 유지보수 가능한 상태로 정리

## 2) 오늘 반영된 핵심 변경

### 2-1. MCPForUnity 중복 충돌 정리
- `Assets/MCPForUnity` 전체 제거
- `Packages/com.coplaydev.unity-mcp`만 유지
- 효과:
  - GUID 충돌 대량 로그 제거
  - `MCPForUnity.Editor/Runtime asmdef` 중복 오류 제거

### 2-2. 아이템 프로토타입 자동 시작 제어
- `ItemPrototypeHotkeyRunner` 자동 부트스트랩 토글 변수 유지
- 현재 코드 기준 `EnableAutoBootstrapOnPlay = true` 상태로 동작

### 2-3. 화염방사기 VFX 동작 개선
- 이전 문제:
  - 1회성/고정형 이펙트처럼 보임
  - 머티리얼 미스매치 시 핑크/검은 배경 이슈
- 현재 동작:
  - 플레이어 전방에서 연속 분사형 파티클 연출
  - 데미지는 기존대로 `OverlapCapsule` 범위 판정 기반(틱 데미지)
  - 파티클 렌더러 머티리얼은 `VFX_ITEM_FLAME` 에셋 머티리얼을 우선 복제해서 사용
  - 에셋 로드 실패 시 fallback 머티리얼/텍스처로 자동 대체

### 2-4. VFX 정책 변경 (요청 반영)
- 화염방사기 외 아이템은 VFX 비활성화
- 적용 방식:
  - `PlayItemUsePresentation()`에서 `ITEM_FLAMETHROWER`만 start VFX 허용

### 2-5. 스턴 게이지/체력 게이지 분리 반영
- CSV 반영:
  - `ItemTable.csv`에 `stunDamage` 컬럼 추가
  - 아이템별 스턴 누적값을 별도로 설정 가능
- 로더/아이템 로직 반영:
  - `ItemTableCsvLoader.Row.StunDamage` 추가
  - `ItemMasterCsvLoader.Row.StunDamage` 추가
  - `FlamethrowerTickRequest`에 `StunDamagePerTick` 추가
- 프로토타입 더미 반영:
  - `PrototypeDamageDummy`에 체력/스턴 게이지 분리
  - 스턴 누적 임계치 도달 시 `isStunned=true` 진입 후 일정 시간 뒤 해제
  - HP 0 도달 시 `DeathCount` 증가 후 HP 즉시 리셋(테스트 루프 유지)
- UI 반영:
  - 우측 패널에 `DummyStun`, `StunCount`, `DeathCount`, `FlameLastStun` 표시

### 2-6. 추가 반영 (스턴 게이지 질의 대응)
- 요청 배경:
  - "스턴 게이지/체력 게이지가 CSV와 아이템 로직에 있는가?" 질의 대응
- 최종 상태:
  - `ItemTable.csv` + CSV 로더 + 화염 틱 로직 + 더미 판정 + 우측 디버그 UI까지 모두 연결 완료
  - 체력 피해(HP)와 스턴 누적(STUN)을 분리해서 확인 가능

## 3) 현재 게임플레이 기준 동작 요약
- 화염방사기:
  - 시각: 전방 분사형 파티클 (지속 생성)
  - 판정: 범위 내 대상 체력/스턴 동시 누적 (틱 처리)
- 기타 아이템:
  - SFX는 유지
  - VFX는 의도적으로 비활성화

## 4) 검증 결과
- Unity 스크립트 검증: 에러 없음
- Unity 리프레시/컴파일: 에러 없음
- 콘솔 재검증(이번 수정 직후, 콘솔 클리어 후 재컴파일 기준):
  - `[WebSocket] Unexpected receive error: WebSocket is not initialised` 1건만 확인
  - `Missing Script` 로그는 이번 재검증 구간에서는 미재현

## 5) 다음 세션 우선 TODO
1. 누락 스크립트(`Unknown`) 붙은 오브젝트 식별 후 정리
2. 화염방사기 파티클 아트 품질 튜닝
   - 분사 폭(angle), 입자량(rate), 크기(size), 색상/밝기
3. 비-화염 아이템용 신규 VFX 에셋 확보 후 테이블 재연결
4. 필요 시 화염방사기 연출을 프리팹 풀링 방식으로 전환

## 6) 빠른 확인 절차
1. Unity Play 진입
2. `5` 입력
3. 전방 분사형 연출 + 범위 틱 HP/스턴 누적 확인
4. 우측 UI의 `DummyStun`, `StunCount`, `DeathCount` 변화 확인
5. `1,2,3,4,6,7` 입력 시 VFX 미출력 확인 (SFX/로직 동작은 유지)

## 7) 참고 파일
- `client/Assets/_Project/Scripts/Game/ItemPrototypeHotkeyRunner.cs`
- `client/Assets/_Project/Scripts/Game/ItemPrototypeHotkeyRunner_Gameplay.cs`
- `client/Assets/_Project/Scripts/Game/ItemPrototypeHotkeyRunner_Presentation.cs`
- `client/Assets/_Project/Scripts/Game/ItemPrototypeHotkeyRunner_TargetingData.cs`
- `client/Assets/_Project/Scripts/Game/ItemPrototypeHotkeyRunner_VisualsUI.cs`
- `client/Assets/_Project/Scripts/Game/PrototypeDamageDummy.cs`
- `client/Assets/_Project/Scripts/Game/ItemTableCsvLoader.cs`
- `client/Assets/_Project/Scripts/Game/Item/ItemMasterCsvLoader.cs`
- `client/Assets/_Project/Scripts/Game/Item/ItemModels.cs`
- `client/Assets/_Project/Scripts/Game/Item/ItemRuntimeController.cs`
- `client/Assets/_Project/Data/ItemTable.csv`

